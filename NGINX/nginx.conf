events{}
http{
    upstream aspnetapp{
        server 127.0.0.1:80;
        # server [private-ip-of-host-2]:80; add a second VM IP to use NGINX as a load balancer too
    }
    server {
        listen 80;
        location / {
            proxy_pass http://127.0.0.1;
        }
    }
}
Reload NGINX.